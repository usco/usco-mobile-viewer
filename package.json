{
  "name": "foobase",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify src/feedback.js -o dist/index.js -t [ babelify]",
    "watch": "watchify src/bunny_6.js -o dist/index.js -t [ babelify]",
    "start-dev": "budo src/distField.js --port=8080 --live -- -t babelify"
  },
  "author": "Mark 'kaosat-dev' Moissette",
  "license": "MIT",
  "dependencies": {
    "bunny": "^1.0.1",
    "gl-mat4": "^1.1.4",
    "glslify": "^5.0.2",
    "mouse-change": "^1.2.1",
    "regl": "github:mikolalysenko/regl#gh-pages"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-core": "^6.2.1",
    "babel-preset-es2015": "^6.1.18",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "watchify": "^3.7.0"
  },
  "browserify": {
    "transform": [
      "glslify"
    ]
  }
}
